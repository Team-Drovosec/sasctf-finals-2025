# Random Program Generator & Compiler

This module generates random C programs based on predefined patterns and compiles them using a Makefile-based build system.

Example usage:

```bash
python -m proggen generate --make-dir=/home/user/sasctf-development/sasctf-finals-2025/gatekeeper/service/example_program --target read_loop --seed 1234312
```
