# Trusted Lib

This module provides safe, verifier-adapted implementations of some standard C library functions. The whole library is compiled as a static object, and is expected to be placed at address `0x80000000`, for the use-code to use.
